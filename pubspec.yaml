name: ev_nepal
description: "EVCharging - Find. Book. Charge. Swap."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.4
  http: ^1.2.2
  provider: ^6.1.2
  qr_flutter: ^4.1.0
  intl: ^0.20.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  geolocator: ^13.0.2
  mobile_scanner: ^5.2.3
  url_launcher: ^6.3.0
  cached_network_image: ^3.3.1
  image_picker: ^1.1.2
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
